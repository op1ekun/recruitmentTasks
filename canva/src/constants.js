export const DEFAULT_WIDTH = (window.innerWidth - 20) / 2;
export const DEFAULT_HEIGHT = (window.innerHeight - 20) / 2;
// to avoid using magic strings
export const supportedEvents = {
    IMAGE_RENDERED: 'image-rendered'
}
